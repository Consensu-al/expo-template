/**
 * Database constants for the application
 * These settings control how SQLite database is configured
 */

// The main database file name
// This should be a simple name without path elements
export const DATABASE_NAME = "consensual-app.db";

// Database version for schema migrations
export const DB_VERSION = 1;

// Human-readable database description 
export const DB_DESCRIPTION = "Consensual App Database";
